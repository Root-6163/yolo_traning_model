# yolo_traning_model
yolo traning model is hackthon task to train raw data of images.

# Running on Linux 
install python3 
apt install python3

install libraries 
pip install -r requirements.txt

run a model 
python3 train.py
